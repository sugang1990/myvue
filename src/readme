--assets 目录为静态资源目录，存放公用图片，样式文件，js插件
--components 目录为公共组件目录，多个页面共用的组件可以放在该目录，例如 dialog弹框组件，滚动条组件，共用的页面头部，导航等等
--page 目录为业务目录，系统分几个模块，每个模块包含几个部分
--router/index.js 是路由配置，每增加一个页面都需要在路由里面新增配置
--service 目录定义访问后台接口，按照模块划分，一个模块写一个接口文件
--tool 自己写的一些公用方法文件
--App.vue 系统主页面，一般情况下都是一个路由
--main.js 系统主入口文件
